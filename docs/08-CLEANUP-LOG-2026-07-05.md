# 🧹 Cleanup Log — 2026-07-05

This log records **one** cleanup pass on 2026-07-05. It is a historical
snapshot, not a maintenance policy — the current cleanup policy lives
in [`scripts/cleanup-local-ai`](../scripts/cleanup-local-ai) and is
documented in [`docs/04-TROUBLESHOOTING.md`](04-TROUBLESHOOTING.md).

## Removed locally

| Path | Why |
| --- | --- |
| `~/ai/models/qwen3-14b` | Obsolete local model; no active profile points to Qwen 14B anymore. |
| `~/ai/finalize-local-ai-stack.py.broken` | Broken setup leftover from the early stack build. |
| `~/codebase/local-ai-gmail-interpreter/logs` | Historical run logs after the mail cleanup completed. |
| `~/codebase/sorter/reports/*` | Generated private mailbox reports; ignored by Git and rebuildable. |
| `~/codebase/sorter/manifests/*` | Generated private action manifests; ignored by Git and rebuildable. |
| `~/codebase/sorter/data/*` | Generated progress/state cache; ignored by Git and rebuildable. |
| `__pycache__` and `.pytest_cache` under sorter | Python cache output. |

`.gitkeep` placeholders were kept in `reports/`, `manifests/`, and
`data/` so the directory structure survives in GitHub without
private generated content.

## Kept locally

| Path | Why |
| --- | --- |
| `~/ai/models/qwen3.6-35b-a3b-mtp` | Active primary local coding model. |
| `~/ai/models/deepseek-r1-qwen-32b` | Active reasoning fallback model. |
| `~/ai/models/gemma4-26b-moe` | Installed planner target. |
| `~/ai/models/gemma4-12b` | Installed planner fallback. |
| `~/ai/llama.cpp` | Required runtime and build output for the local server. |
| `~/codebase/sorter/secrets/*.json` | Gmail OAuth credentials/tokens; ignored by Git and intentionally not touched. |

## Service state

`local-llm.service` was stopped and disabled so the machine does not
automatically consume VRAM or try to start a model unless a helper
command explicitly requests it.

## Follow-up model installs

After the cleanup, the planner profiles were repointed at the official
GGUF filenames:

```text
~/ai/models/gemma4-26b-moe/gemma-4-26B-A4B-it-Q4_K_M.gguf
~/ai/models/gemma4-12b/gemma-4-12B-it-Q4_K_M.gguf
```

Both are installed; both still need a fair local fit/speed benchmark
before being treated as the daily planner default.
