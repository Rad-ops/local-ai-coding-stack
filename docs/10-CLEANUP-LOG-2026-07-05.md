# 🧹 Cleanup Log - 2026-07-05

## Removed Locally

| Path | Why |
| --- | --- |
| `~/ai/models/qwen3-14b` | Obsolete local model; no active profile should point to Qwen 14B anymore. |
| `~/ai/finalize-local-ai-stack.py.broken` | Broken setup leftover. |
| `~/codebase/local-ai-gmail-interpreter/logs` | Historical run logs after mail cleanup completed. |
| `~/codebase/sorter/reports/*` | Generated private mailbox reports; ignored by Git and rebuildable. |
| `~/codebase/sorter/manifests/*` | Generated private action manifests; ignored by Git and rebuildable. |
| `~/codebase/sorter/data/*` | Generated progress/state cache; ignored by Git and rebuildable. |
| `__pycache__` and `.pytest_cache` under sorter | Python cache output. |

The cleanup restored only `.gitkeep` placeholders for `reports/`, `manifests/`, and `data/` so GitHub keeps the directory structure without private generated content.

## Kept Locally

| Path | Why |
| --- | --- |
| `~/ai/models/qwen3.6-35b-a3b-mtp` | Active primary local coding model. |
| `~/ai/models/deepseek-r1-qwen-32b` | Active reasoning fallback model. |
| `~/ai/llama.cpp` | Required runtime and build output for the local server. |
| `~/codebase/sorter/secrets/*.json` | Required Gmail OAuth credentials/tokens; ignored by Git and intentionally not touched. |

## Service State

`local-llm.service` was stopped and disabled so the machine does not automatically consume VRAM or try to start a model unless a helper command requests it.

## Follow-Up Model Installs

The planner profiles now point to official GGUF filenames:

```text
~/ai/models/gemma4-26b-moe/gemma-4-26B-A4B-it-Q4_K_M.gguf
~/ai/models/gemma4-12b/gemma-4-12B-it-Q4_K_M.gguf
```

Install and benchmark these before treating them as active.
